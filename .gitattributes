# LFS disabled — chroma_db is gitignored (rebuilt via build_github_vectordb).
# Other data files (NGC catalog, GitHub READMEs, log fixtures) are small enough
# for regular git storage. GitHub free LFS budget too tight for portfolio repo use.
