__pycache__/
*.pyc
.env
.venv/
venv/
output/*
!output/.gitkeep
*.egg-info/
.pytest_cache/
test_responses.txt

# Transient eval / troubleshoot run logs (per-invocation; curated baselines
# live under data/eval_runs/). Pattern covers opus_X_run.log, X_eval_run.log,
# bare eval_run.log, run.log, etc.
*run*.log
!data/eval_runs/**

# Chroma vector DB — rebuild via `python -m ingest.build_github_vectordb`
# (kept out of git to avoid repo bloat; corpus JSONL is committed for rebuild input)
data/chroma_db/

# Superpowers brainstorming artifacts (visual companion mockups)
.superpowers/
